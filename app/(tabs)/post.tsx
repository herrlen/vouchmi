// Dummy-Screen — wird nie gerendert. Custom tabBarButton im _layout fängt
// den Tab-Press ab und navigiert zu /create-post.
import { View } from "react-native";
export default function PostTab() {
  return <View />;
}
